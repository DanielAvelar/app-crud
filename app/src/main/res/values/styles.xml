<resources>

    <style name="AppThemeSplashScreen" parent="Theme.AppCompat.DayNight">
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorBackground">@drawable/gradient_background</item>
    </style>

    <style name="AppThemeNoBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/bg_list_item</item>
        <item name="android:background">@color/bg_list_item</item>
    </style>

</resources>
